function KastEkko (triggerPin: number, ekkoPin: number) {
    pins.analogWritePin(AnalogPin.P0, triggerPin)
}
function SjekkAvstand (triggerPin: number, ekkoPin: number) {
    pins.analogWritePin(AnalogPin.P0, triggerPin)
}
